// Delaring variables
const john = 'john'
const pete = 'pete'

//declaring function sayHi which has the parameter 'name'

const sayHi = (name) => {   
    console.log(`hello there ${name}`)
}

//calling function and passing values for parameter

sayHi('susan') //passing direct value
sayHi(john) //passing variable
sayHi(pete)